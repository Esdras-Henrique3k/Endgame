#include <iostream>
#include <vector>

class rota{
   
  string origem;
  string destino;
  int horario;
  double preco;
  int paradas;
  onibus tipo;
    
  public:
   
rota(string _origem, string _destino,vector<int> _horario, double _preco, int _paradas, onibus _tipo);
  
};

 
