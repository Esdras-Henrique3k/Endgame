#include "adm.hpp"


adm::adm(){}

void adm::incrementarCaixa(){
  
}

bool user::painel(){
  bool stream = true;
  int x; std::string y;
  while(stream){
    std::cout<<"Digite uma das opcoes 'adicionar rota' | 'remover rota' | 'exit': ";
    std::cin>>y;
    if(y=="adionar rota"){                    }else{std::cout<<"opcao invalida!"};
    if(y=="remover rota"){                    }else{std::cout<<"opcao invalida!"};
    if(y=="exit"){ stream=false;              }else{std::cout<<"opcao invalida!"};
               }
  std::cout<<"Digite uma das opcoes (adicionar rota / remover rota / exit): ";
  std::cin>>y;

  
  
  
  
  
}